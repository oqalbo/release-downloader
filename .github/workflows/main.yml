name: Download and Inject into Tag Archive

on:
  workflow_dispatch:
    inputs:
      target_repo:
        description: 'Target repository (owner/repo)'
        required: true
      release_tag:
        description: 'Release tag from target'
        required: true
      file_pattern:
        description: 'File to download (e.g., *.apk)'
        required: true

jobs:
  inject:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download target asset
        uses: robinraju/release-downloader@v1.10
        with:
          repository: ${{ github.event.inputs.target_repo }}
          tag: ${{ github.event.inputs.release_tag }}
          fileName: ${{ github.event.inputs.file_pattern }}
          out-file-path: "downloaded"

      - name: Create git repo with the file as source
        run: |
          # Create empty repo structure
          mkdir repo_content
          cp downloaded/* repo_content/
          
          # Initialize git
          git init
          cp -r repo_content/* .
          git add .
          git commit -m "Add downloaded file"
          
          # Create a tag that points to this commit
          git tag "pass-${{ github.run_id }}"

      - name: Push tag to your repository
        run: |
          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git push origin "pass-${{ github.run_id }}"
